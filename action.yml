name: 'valar-action'
description: 'GitHub Action for Valar'
author: 'Valar'
branding:
  icon: 'upload-cloud'
  color: 'red'
inputs:
  token:
    description: 'Valar token'
    required: true

runs:
  using: 'node16'
  main: 'lib/main.js'
